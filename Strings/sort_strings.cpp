#include <bits/stdc++.h>

using namespace std;

int main(){

    int n,col,temp;
    cin >> n;
    vector<string> str;
    for(int i=0; i<n; i++){
        cin >> temp;
        str.push_back(temp);
    }

    string which_order,is_reverse;
    cin >> col >> is_reverse >> which_order;


    return 0;
}
